import Link from 'next/link'; import {readMatchspec} from '../../lib/contract'; import {displayValue} from '../../lib/presentation';
export const dynamic='force-dynamic';
type Item={id:number;manufacturer:string;product_name:string;kind:string;model_number:string;revision:string};
export default async function Items(){let items:Item[]=[];let error='';try{const n=await readMatchspec('get_item_count') as number;items=n?await readMatchspec('get_items',[0,50]) as Item[]:[]}catch(e){error=e instanceof Error?e.message:'Unable to read contract.'}return <><div className="eyebrow">MATCHSPEC / ITEM CATALOGUE</div><h1 className="title">Registered components.</h1><div className="toolbar"><span className="mono">{items.length} ITEMS / LIVE CONTRACT READ</span><Link href="/items/new"><button>REGISTER ITEM</button></Link></div>{error?<div className="notice" role="alert">LIVE CONTRACT READ ERROR — {error}</div>:!items.length?<div className="notice">No items are registered on the configured contract.</div>:<table className="table"><thead><tr><th>ID</th><th>MANUFACTURER</th><th>PRODUCT</th><th>KIND</th><th>MODEL / REVISION</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.id}</td><td>{i.manufacturer}</td><td>{i.product_name}</td><td>{i.kind}</td><td>{i.model_number} / {i.revision}</td></tr>)}</tbody></table>}</>}
