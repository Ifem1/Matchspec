'use client';
import {createContext,useContext,useEffect,useState} from 'react';
import {connectWallet,Eip1193,STUDIONET} from '../lib/wallet';
type Wallet={address:string;chainId:string;error:string;connect:()=>Promise<void>;switchToStudionet:()=>Promise<void>};
const Context=createContext<Wallet>({address:'',chainId:'',error:'',connect:async()=>{},switchToStudionet:async()=>{}});
export function WalletProvider({children}:{children:React.ReactNode}){const [address,setAddress]=useState('');const [chainId,setChainId]=useState('');const [error,setError]=useState('');const provider=()=>{const p=(window as Window & {ethereum?:Eip1193}).ethereum;if(!p)throw new Error('An injected EIP-1193 wallet is required.');return p};const switchToStudionet=async()=>{try{await provider().request({method:'wallet_switchEthereumChain',params:[{chainId:STUDIONET.chainId}]});setChainId(STUDIONET.chainId);setError('')}catch(e){try{await provider().request({method:'wallet_addEthereumChain',params:[STUDIONET]});setChainId(STUDIONET.chainId);setError('')}catch(err){setError(err instanceof Error?err.message:'Studionet switch rejected.')}}};const connect=async()=>{try{const a=await connectWallet();setAddress(a);setChainId(String(await provider().request({method:'eth_chainId'})));setError('')}catch(e){setError(e instanceof Error?e.message:'Wallet connection failed')}};useEffect(()=>{const p=(window as Window & {ethereum?:Eip1193}).ethereum;if(!p)return;const account=(x:unknown)=>setAddress(Array.isArray(x)?String(x[0]||''):String(x||''));const chain=(x:unknown)=>setChainId(String(x));void p.request({method:'eth_accounts'}).then(account);void p.request({method:'eth_chainId'}).then(chain);p.on?.('accountsChanged',account);p.on?.('chainChanged',chain);return()=>{p.removeListener?.('accountsChanged',account);p.removeListener?.('chainChanged',chain)}},[]);return <Context.Provider value={{address,chainId,error,connect,switchToStudionet}}>{children}</Context.Provider>}
export const useWallet=()=>useContext(Context);
export const isStudionet=(id:string)=>id===STUDIONET.chainId;
