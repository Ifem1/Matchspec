'use client';
import {useWallet,isStudionet} from './WalletProvider';
export default function WalletButton(){const {address,chainId,error,connect,disconnect,switchToStudionet}=useWallet();return <div className="wallet-control"><div>{address?<><span className="mono">{address.slice(0,6)}…{address.slice(-4)}</span><button onClick={disconnect}>DISCONNECT</button></>:<button onClick={()=>void connect()}>CONNECT WALLET</button>}</div>{address&&!isStudionet(chainId)&&<button onClick={()=>void switchToStudionet()}>SWITCH TO STUDIONET</button>}{error&&<p role="alert" className="mono">{error}</p>}</div>}
